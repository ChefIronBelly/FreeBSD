# FreeBSD-15-CURRENT

# start w / FreeBSD-15.0-CURRENT-amd64-*-mini-memstick.img.xz

default install using UFS default sizes

# pkg install sudo mc mksh bash gh cpu-microcode-amd
# visudo enable wheel
# vi /etc/group add user to wheel and video
logout

****
as user
$ chsh /usr/local/bin/mksh
$ sudo pkg install xorg drm-kmod pkgconf liberation-fonts-ttf

$ mkdir ~/Source & cd Source
$ git clone https://github.com/ChefIronBelly/FreeBSD.git
$ cp user files and root files from /boot /etc /home to system

$ cd Source /dwm /dmenu /st and sudo make clean install
$ sudo mkdir /mnt/usb

$ sudo pkg install firefox geany
$ sudo pkg install mpg123 mpv yt-dlp scrot freecolor fastfetch 
