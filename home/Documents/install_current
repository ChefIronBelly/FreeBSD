# FreeBSD-16-CURRENT

# start w / FreeBSD-16.0-CURRENT-amd64-*-mini-memstick.img.xz

default install using UFS default sizes

# pkg install xorg-minimal drm-66-kmod libXft xrdb xset xsetroot setxkbmap xmodmap doas cpu-microcode-amd
pkgconf mksh

# echo permit persist :wheel > /usr/local/etc/doas.conf
# pw usermod user -G wheel,video or use installer at user create
logout

****
as user
$ chsh /usr/local/bin/mksh
$ doas pkg install  firefox geany mc mpv yt-dlp mpg123 gh liberation-fonts-ttf
scrot freecolor fastfetch

$ mkdir ~/Source & cd Source
$ git clone https://github.com/ChefIronBelly/FreeBSD.git
$ cp user files and root files from /boot /etc /home to system as needed

$ cd Source /dwm /dmenu /st and doas make clean install
$ doas mkdir /mnt/usb

# 260619
